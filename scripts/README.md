# Betriebsskripte

Geplant sind `bootstrap.sh`, `doctor.sh`, `dev.sh`, `test.sh`, `backup.sh`, `restore.sh`, `set-admin-password.sh` und `wait-for-health.sh`. Skripte werden erst mit realer Funktion angelegt, bleiben idempotent und geben keine Secrets aus.
