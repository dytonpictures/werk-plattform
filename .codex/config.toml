# Projektbezogene Codex-Einstellungen.
# Wird nur geladen, wenn dieses Repository als vertrauenswürdig markiert ist.
# Persönliche Einstellungen, Zugangsdaten und Provider-Konfiguration gehören
# bewusst nicht in diese Datei.

[agents]
# Kleine, kontrollierbare Parallelität für klar unabhängige Lese- oder Reviewarbeit.
max_threads = 3
max_depth = 1
interrupt_message = true
