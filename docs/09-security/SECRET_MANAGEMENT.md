# Secret-Management

Secrets werden über lokale, nicht eingecheckte Umgebungsdateien oder eine spätere Secret-Lösung injiziert. `.env.example` enthält ausschließlich Platzhalter. Secrets erscheinen weder in Argumentlisten, Images, Logs, Dokumentation noch Test-Fixtures.
