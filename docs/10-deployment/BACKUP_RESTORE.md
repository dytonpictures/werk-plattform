# Backup und Wiederherstellung

Backups müssen PostgreSQL konsistent sichern, verschlüsselt und zugriffsgeschützt abgelegt sowie regelmäßig durch Wiederherstellung in einer separaten Umgebung geprüft werden. Restore-Vorgänge sind explizit, protokolliert und überschreiben nie stillschweigend Daten.
