# Go-Richtlinien

Verbindlich sind `gofmt`, `go test ./...`, `go vet`, explizite Konstruktoren, durchgereichter `context.Context`, kleine Interfaces am Verbraucher und sichere Transaktionen. Globale Datenbanken, erwartbare Panics und ignorierte Fehler sind unzulässig.
