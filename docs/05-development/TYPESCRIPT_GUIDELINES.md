# TypeScript-Richtlinien

TypeScript läuft strikt und ohne unkontrolliertes `any`. Server-/Client-Grenzen werden bewusst gewählt, API-Typen zentral gepflegt und Lade-, Fehler- sowie Leerzustände umgesetzt. Autorisierung findet nicht ausschließlich im Client statt.
