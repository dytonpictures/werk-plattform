# Coding Standards

Änderungen bleiben klein, typisiert, testbar und an Modulgrenzen ausgerichtet. Fehler werden explizit behandelt. Konfiguration kommt aus validierter Umgebung, strukturierte Logs enthalten keine Secrets, und öffentliche APIs werden gemeinsam mit OpenAPI geändert.
