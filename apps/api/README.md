# API-Anwendung

Hier entsteht der Go-basierte modulare Monolith. Einstiegspunkte liegen unter `cmd/`, Fachlogik unter `internal/`, Schemaänderungen unter `migrations/` und die API-Beschreibung unter `openapi/`.
